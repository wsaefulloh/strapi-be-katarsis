'use strict';

/**
 * activation-service router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::activation-service.activation-service');
